<p style="color: green"><%= notice %></p>

<h1  class="test">Lists</h1>

<div id="lists">
  <% @lists.each do |list| %>
    <%# <%= render list %>
    <div class="card-category">
      <%= link_to list.name, list_path(list) %>
    </div>
  <% end %>
</div>

<%= link_to "New list", new_list_path %>
